{
  "name": "bulletproof-journal-backend",
  "version": "1.0.0",
  "description": "Backend API server for Bulletproof Journal mobile application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "init-db": "node -e \"require('./src/database/init').initializeDatabase().then(() => process.exit(0)).catch(err => {console.error(err); process.exit(1)})\""
  },
  "keywords": [
    "journal",
    "diary",
    "mobile",
    "react-native",
    "express",
    "sqlite"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@supabase/supabase-js": "^2.93.3",
    "cors": "^2.8.6",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "morgan": "^1.10.1",
    "multer": "^2.0.2",
    "pg": "^8.17.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
